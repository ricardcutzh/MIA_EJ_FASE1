#include<general.h>
#include<listatoken.h>
#include<analizadorlexico.h>
#include<analizadorsintactico.h>
#include<consola.h>

int main()
{
    iniciaConsola();
    /*lista *l = nuevaLista();
    token *n1 = newToken("primero","primero");
    token *n2 = newToken("segundo","segundo");
    token *n3 = newToken("tercero","tercero");
    token *n4 = newToken("cuarto","cuarto");
    inserta(l, n1);
    inserta(l, n2);
    inserta(l, n3);
    inserta(l, n4);
    imprime(l);
    deleteLista(l);
    imprime(l);*/
    return 0;
}
